# Templates locais (não versionados)

Planilhas e modelos de importação ficam aqui apenas no ambiente de desenvolvimento.
Arquivos `*.xlsx` são ignorados pelo `.gitignore` (podem conter dados internos do projeto).

Não commite `planning_poker_import.xlsx` nem outros `.xlsx` desta pasta.
